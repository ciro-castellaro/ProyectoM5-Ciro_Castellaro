# GitHub Personal Access Token (classic). Scopes minimos: repo, user
# (admin:org solo si se van a soportar operaciones sobre organizaciones).
GITHUB_TOKEN=

# Nivel de logging: debug | info | warn | error
LOG_LEVEL=info
